# External Dependencies

This workspace depends on sibling repositories in the thesis root.

Expected locations:

- `../repos/lerobot`
- `../repos/X-VLA`
- `../repos/SO-ARM100`

These are treated as external dependencies rather than part of the internal workspace layout.
