# XVLA Delta Action Notes

This document was retained during the repo restructuring as an architecture note.

The active implementation now lives under the `src/thesis_vla/` and `apps/` layout rather than the legacy flat workspace layout.
